[tox]
envlist = lint
skipsdist = true

[testenv:lint]
deps = ruff
commands =
    ruff check {posargs:.}
    ruff format --check {posargs:.}

[testenv:format]
deps = ruff
commands =
    ruff format {posargs:.}
    ruff check --fix {posargs:.}
